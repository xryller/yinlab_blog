version: '3.8'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./site:/app/site
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
