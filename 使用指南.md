# 📝 MPNN 博客系统 - 使用指南

## 🎯 功能特性

### ✅ 已完成的所有功能 (A-E)

#### A - 自动重试/修复逻辑
- ✨ **智能解析**：当 DeepSeek 返回格式异常时，自动发起第二次严格 JSON-only 请求
- 🔄 **降级策略**：如果两次都失败，自动使用合并文本作为内容
- 📊 **日志跟踪**：所有解析步骤都有详细日志输出

#### B - DeepSeek 函数调用 (response_format)
- ✅ **JSON 模式**：使用 `response_format={"type": "json_object"}` 强制返回结构化数据
- 🎯 **提高准确性**：配合严格的 prompt 指令，大幅提升 JSON 格式遵循率
- 🔧 **容错处理**：即使启用 JSON 模式，仍保留代码块清理逻辑

#### C - 优化编辑器和 OpenWebUI 集成
- 🎨 **全新管理界面**：卡片式布局，支持编辑、预览、OpenWebUI 打开
- ✏️ **可视化编辑器**：内联编辑标题、摘要、正文、标签
- 🤖 **AI 优化按钮**：一键使用 DeepSeek 重写优化现有内容
- 🔗 **OpenWebUI 集成**：通过 iframe 和 postMessage 实现双向内容同步

#### D - 强化 Prompt 指令
- 📋 **微信公众号风格**：专业指导生成高质量中文技术博文
- 🎯 **严格格式要求**：9 项详细规范，包含字数、结构、风格指导
- ⚠️ **警告标记**：使用 emoji 和重复强调，提醒 AI 必须返回纯 JSON

#### E - 部署测试和浏览器预览
- 🚀 **一键部署**：Docker Compose + 交互式 setup.sh 脚本
- 🌐 **浏览器预览**：已在 VS Code Simple Browser 打开首页
- ✅ **健康检查**：`/health` 端点验证服务运行状态

---

## 🚀 快速开始

### 1. 初始配置

```bash
# 克隆或进入项目目录
cd /workspaces/hzau-MPNN_fr

# 运行交互式配置脚本
bash setup.sh
# 按提示输入 DeepSeek API Key: sk-c19847906874408e86a65a8fa6f5dc3c
# 其他参数使用默认值即可

# 或手动创建 .env 文件
cat > .env << 'EOF'
DEEPSEEK_API_KEY=sk-c19847906874408e86a65a8fa6f5dc3c
DEEPSEEK_API_URL=https://api.deepseek.com
OPENWEBUI_URL=http://localhost:3000
EOF
```

### 2. 启动服务

```bash
# 启动 Docker 容器
docker compose up -d

# 查看日志
docker compose logs -f api

# 验证服务健康
curl http://localhost:8000/health
# 应返回: {"status":"ok"}
```

### 3. 访问界面

- 📰 **博客首页**: http://localhost:8000/
- 🔧 **管理后台**: http://localhost:8000/admin
- 📄 **API 文档**: http://localhost:8000/docs (FastAPI 自动生成)

---

## 📤 上传文件生成博文

### 方法 1: Web 界面上传 (推荐)

1. 访问 http://localhost:8000/
2. 在上传表单中：
   - 输入分类（例如："深度学习"）
   - 点击"选择文件"，支持多选（PDF, Word, PPT, TXT）
   - 点击"上传"
3. 等待 AI 生成（通常 10-30 秒）
4. 自动跳转到新生成的博文页面

### 方法 2: 命令行上传

```bash
# 单文件上传
curl -X POST -F "files=@你的文件.pdf" -F "category=技术分享" \
  http://localhost:8000/upload

# 多文件上传（模拟文件夹）
curl -X POST \
  -F "files=@paper.pdf" \
  -F "files=@slides.pptx" \
  -F "files=@discussion.docx" \
  -F "category=学术研究" \
  http://localhost:8000/upload

# 返回示例
# {"ids":["abc123","def456"],"url":"/site/xyz789.html","slug":"xyz789"}
```

### 方法 3: 服务器端导入（绕过上传限制）

```bash
# 1. 将文件放到 uploads/ 目录
cp 大文件.pdf /workspaces/hzau-MPNN_fr/uploads/

# 2. 调用 import 接口
curl -X POST -H "Content-Type: application/json" \
  -d '{"filename":"大文件.pdf"}' \
  http://localhost:8000/import
```

---

## ✏️ 编辑和优化博文

### 管理后台功能

访问 http://localhost:8000/admin，你会看到：

1. **文章卡片**
   - 显示标题、日期、分类
   - 提供三个操作按钮

2. **✏️ 编辑按钮**
   - 打开内联编辑器
   - 可修改标题、摘要、正文（Markdown）、标签
   - 💾 保存：更新 JSON 和 HTML
   - ✨ AI 优化：调用 DeepSeek 重写优化内容
   - 取消：关闭编辑器

3. **👁️ 预览按钮**
   - 在新标签页打开生成的 HTML 文章
   - 查看实际渲染效果

4. **🚀 OpenWebUI 按钮**
   - 在 iframe 中打开 OpenWebUI 编辑器
   - 自动预填充当前文章内容
   - 支持从 OpenWebUI 导出保存

### AI 优化流程

1. 在管理后台点击"✏️ 编辑"
2. 可选：先手动调整部分内容
3. 点击"✨ AI 优化"按钮
4. 等待 10-20 秒，AI 会：
   - 改进标题的吸引力
   - 精炼摘要
   - 重组正文结构，增强可读性
   - 补充段落分隔和格式化
5. 检查 AI 生成的内容
6. 点击"💾 保存"应用更改

---

## 🎨 样式特性（微信公众号风格）

生成的博文具有以下特性：

### 视觉设计
- 🎨 **渐变标题栏**：紫色渐变背景，白色文字
- 💎 **毛玻璃标签**：半透明背景 + 模糊效果
- 📦 **摘要卡片**：蓝色渐变边框 + 高亮背景
- 🖼️ **响应式布局**：最大宽度 740px，移动友好

### 内容格式
- 📝 **段落间距**：自动留空行，每段 2-5 行
- 🔤 **中文字体**：PingFang SC / Microsoft YaHei 优先
- 💡 **引用块**：浅灰背景 + 蓝色左边框 + 斜体
- 🖥️ **代码高亮**：深色背景 + 等宽字体
- 📊 **表格支持**：边框 + 斑马纹

### 结构规范
- **引言**（1-2 段）：引出话题，说明重要性
- **核心内容**（2-4 个二级标题 ##）：要点 + 代码 + 公式
- **实践应用**：真实案例 + 最佳实践
- **总结与展望**：归纳 + 未来方向

---

## 🔧 API 端点

### 核心接口

| 端点 | 方法 | 说明 | 参数 |
|------|------|------|------|
| `/` | GET | 博客首页，展示所有文章 | - |
| `/upload` | POST | 上传文件生成博文 | `files` (多个), `category` |
| `/import` | POST | 服务器端文件导入 | `{"filename":"xxx.pdf"}` |
| `/admin` | GET | 管理后台界面 | - |
| `/post/{slug}` | GET | 获取文章 JSON 元数据 | `slug` |
| `/review/{slug}` | POST | 更新文章内容 | JSON body |
| `/api/ai-rewrite` | POST | AI 优化已有内容 | `{title, summary, content, tags}` |
| `/site/{slug}.html` | GET | 访问生成的 HTML 文章 | `slug` |
| `/health` | GET | 健康检查 | - |

### 示例：AI 优化接口

```bash
curl -X POST http://localhost:8000/api/ai-rewrite \
  -H "Content-Type: application/json" \
  -d '{
    "title": "旧标题",
    "summary": "旧摘要",
    "content": "## 原始内容\n\n需要优化的文本...",
    "tags": "标签1, 标签2"
  }'

# 返回优化后的 JSON
# {"title":"优化后的标题","summary":"优化后的摘要","content":"...","tags":[...]}
```

---

## 📁 项目结构

```
hzau-MPNN_fr/
├── app/
│   ├── main.py              # FastAPI 主应用
│   ├── analyzer.py          # DeepSeek AI 集成
│   └── templates/
│       ├── index.html       # 首页卡片布局
│       ├── post.html        # 博文展示页（公众号风格）
│       ├── admin.html       # 管理后台（新版）
│       └── admin_old.html   # 旧版管理界面（备份）
├── uploads/                 # 上传文件存储
├── site/                    # 生成的 HTML 和 JSON
│   ├── {slug}.html          # 渲染后的博文
│   └── {slug}.json          # 元数据（title, summary, tags, content）
├── docker-compose.yml       # 容器编排
├── Dockerfile               # Python 3.11 镜像
├── requirements.txt         # Python 依赖
├── setup.sh                 # 交互式配置脚本
├── .env                     # 环境变量（gitignored）
└── 使用指南.md              # 本文件
```

---

## 🛠️ 故障排查

### 问题 1: 上传后标题显示"未命名博文"

**原因**: DeepSeek 返回了代码块包裹的 JSON 或格式不规范

**解决方案**:
1. 查看日志：`docker compose logs api --tail 100`
2. 系统会自动触发重试逻辑（查找 `[RETRY]` 日志）
3. 如果两次都失败，使用管理后台的"✨ AI 优化"手动修复
4. 或在管理后台手动编辑标题和摘要

### 问题 2: Docker 容器无法启动

**检查步骤**:
```bash
# 查看容器状态
docker compose ps

# 查看错误日志
docker compose logs api

# 验证环境变量
docker compose exec api env | grep DEEPSEEK

# 重启服务
docker compose restart api
```

### 问题 3: AI 生成内容质量不佳

**优化方法**:
1. 确保上传的文件内容清晰、结构完整
2. 提供更具体的分类（例如："深度学习-注意力机制"）
3. 使用管理后台的"✨ AI 优化"重新生成
4. 手动调整 prompt（编辑 `app/analyzer.py` 中的 `system_msg`）

### 问题 4: 上传大文件失败 (413 错误)

**解决方案**:
1. 使用服务器端导入：
   ```bash
   cp 大文件.pdf uploads/
   curl -X POST -H "Content-Type: application/json" \
     -d '{"filename":"大文件.pdf"}' \
     http://localhost:8000/import
   ```
2. 或配置 Nginx/代理服务器提高上传限制

---

## 🎓 高级配置

### 自定义 AI Prompt

编辑 `app/analyzer.py` 中的 `system_msg`：

```python
system_msg = {
    "role": "system",
    "content": (
        "你是一名专业的技术博客编辑..."
        # 修改这里的指令以调整生成风格
    )
}
```

### 调整 AI 参数

在 `app/analyzer.py` 中修改：

```python
resp = client.chat.completions.create(
    model="deepseek-chat",
    messages=[system_msg, user_msg],
    response_format={"type": "json_object"},
    temperature=0.7,  # 创造性：0.0-1.0，越高越随机
    max_tokens=4096,  # 最大输出长度
)
```

### 更换 AI 模型

只需修改 `.env` 文件：

```bash
# 使用其他 OpenAI 兼容 API
DEEPSEEK_API_KEY=your_other_api_key
DEEPSEEK_API_URL=https://api.openai.com  # 或其他兼容端点
```

---

## 📊 性能优化建议

1. **批量上传**：一次上传多个相关文件（PDF + PPT + Word）效果最佳
2. **分类明确**：提供具体的技术分类帮助 AI 理解上下文
3. **文件质量**：使用清晰的扫描版或原生电子文档
4. **定期清理**：`uploads/` 和 `site/` 目录会积累大量文件

---

## 🤝 OpenWebUI 集成说明

### 设置 OpenWebUI

1. 启动 OpenWebUI 容器：
   ```bash
   docker run -d -p 3000:8080 \
     --name openwebui \
     ghcr.io/open-webui/open-webui:main
   ```

2. 在 `.env` 中配置 URL：
   ```bash
   OPENWEBUI_URL=http://localhost:3000
   ```

3. 重启博客服务：
   ```bash
   docker compose restart api
   ```

### PostMessage 协议

#### 从博客到 OpenWebUI (Prefill)
```javascript
// 博客管理后台发送
frame.contentWindow.postMessage({
  type: 'prefill',
  payload: {title, summary, content, tags}
}, OPENWEBUI_ORIGIN)
```

#### 从 OpenWebUI 到博客 (Export)
```javascript
// OpenWebUI 发送
window.parent.postMessage({
  type: 'export',
  payload: {title, summary, content, tags}
}, BLOG_ORIGIN)
```

---

## 📝 总结

你现在拥有一个功能完整的 AI 驱动博客系统：

- ✅ **多文件上传** → 自动合并解析
- ✅ **DeepSeek AI** → 生成结构化中文博文
- ✅ **微信公众号风格** → 美观易读
- ✅ **可视化编辑器** → 内联修改 + AI 优化
- ✅ **OpenWebUI 集成** → iframe + postMessage
- ✅ **自动重试机制** → 提高成功率
- ✅ **JSON 强制模式** → response_format
- ✅ **Docker 部署** → 一键启动

如有问题，查看：
- 📖 **日志**: `docker compose logs api -f`
- 🏥 **健康检查**: http://localhost:8000/health
- 📚 **API 文档**: http://localhost:8000/docs

祝使用愉快！🎉
